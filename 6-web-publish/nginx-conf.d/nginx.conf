server {
  listen 80;
  listen [::]:80;

  root /var/www/html;
  index index.html index.htm;

  server_name swaneet.eu www.swaneet.eu;

  location / {
    proxy_pass http://localhost:8080;
  }

  location ~ /.well-known/acme-challenge {
    allow all;
    root /var/www/html;
  }
}
